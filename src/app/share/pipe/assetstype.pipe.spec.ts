import { AssetstypePipe } from './assetstype.pipe';

describe('AssetstypePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetstypePipe();
    expect(pipe).toBeTruthy();
  });
});
