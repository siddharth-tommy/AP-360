import { OperatorsearchPipe } from './operatorsearch.pipe';

describe('OperatorsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OperatorsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
